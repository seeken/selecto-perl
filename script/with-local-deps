#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
local_lib="$repo_dir/local/lib/perl5"

if [ -n "${PERL5LIB:-}" ]; then
  export PERL5LIB="$local_lib:$repo_dir/lib:$PERL5LIB"
else
  export PERL5LIB="$local_lib:$repo_dir/lib"
fi

exec "$@"
